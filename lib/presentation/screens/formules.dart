import 'package:flutter/material.dart';

class Formules extends StatelessWidget {
  const Formules({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Formules"),
    );
  }
}
